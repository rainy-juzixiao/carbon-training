# 踩坑记录

范围：数据库、后端。更新日期：2026-09-23

---

## 1. 雪花 ID 以 JSON number 返回时精度丢失

**现象：** 接口返回的 id 与数据库中的值不一致，按该 id 查询详情时结果错误。  
**原因：** 主键为约 19 位的雪花 ID（BIGINT），超过 JavaScript 可精确表示的整数范围。后端以 number 类型序列化 Long 后，前端解析会丢失低位精度。  
**处理：** 在实体的 Long 类型字段（主键、外键等）上增加：

```java
@JsonFormat(shape = JsonFormat.Shape.STRING)
@JSONField(serializeUsing = ToStringSerializer.class)
private Long id;
```

代码生成产出的实体可能缺少上述注解，生成后需检查。

---

## 2. `anonymous()` 导致已认证请求返回 403

**现象：** 不带 token 请求返回 200，携带 `Authorization: Bearer` 请求同一接口返回 403。  
**原因：** `SecurityConfig` 中对路径使用了 `anonymous()`。该策略仅允许未通过认证的请求访问，已认证请求会被拒绝。  
**处理：**

- 需要登录才能访问：移除该路径的 `anonymous()`，由 `anyRequest().authenticated()` 处理。
- 允许匿名访问：使用 `permitAll()`，不要使用 `anonymous()`。

修改位于 `neu-framework` 时，需重启后端进程。

---

## 3. `bid_bidding` 标题列名为 `bid_title`

表中不存在 `bidding_title` 列，标题字段为 `bid_title`。  
Mapper 或手写 SQL 使用错误列名会报 `Unknown column`。

---

## 4. 401 与 404 的区分

| 状态码 | 含义 |
|--------|------|
| 401 | 请求未通过认证，或路径不在匿名白名单；不能据此判断接口是否存在 |
| 404 | 请求已通过安全层，当前应用中不存在对应的 mapping |

判断接口是否存在时，应检查运行工程的源码或已编译 class，或携带有效 token 后再请求。

---

## 5. Mapping 路径与调用路径需一致

```java
@GetMapping("/front/list/")  // 与调用方不一致（含尾部斜杠）
@GetMapping("/front/list")   // 与调用方一致
```

Spring Boot 2.x 可能默认匹配尾斜杠，仍应保持 mapping 与调用方路径完全一致。
