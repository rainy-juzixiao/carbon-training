# 代码生成功能使用说明

---

## 1. 使用流程

### 1.1 导入库表

1. 打开 `代码生成` → 点击 `导入`。
2. 弹窗中调用 `GET /tool/gen/db/list` 列出库内表，按表名/描述筛选。
3. 勾选一张或多张表 → 确认。
4. 调用 `POST /tool/gen/importTable?tables=表名1,表名2`。
5. 关闭弹窗后列表刷新（`@ok → handleQuery`）。

### 1.2 修改生成配置

1. 列表勾选一行 → `修改`，或行内编辑图标。
2. 路由：`/tool/gen-edit/index/{tableId}`，组件 `editTable.vue`。
3. 页签通常包括：
   - **基本信息**：类名、作者、所属模块、包路径、业务名、功能名、生成模板（crud / tree）等。
   - **生成信息**：前端模板、作者、生成路径、是否国际化等。
   - **字段信息**：列与 Java 属性映射、插入/编辑/列表/查询、必填、字典类型、控件类型等。
4. 提交：`PUT /tool/gen`（`updateGenTable`），成功后返回列表。

表结构在库中变更后，去列表点行内 `同步数据库`（`GET /tool/gen/synchDb/{tableName}`），刷新字段配置。

### 1.3 预览

行内 `预览` → `GET /tool/gen/preview/{tableId}`。  
返回对象的 key 为模板文件名（如 `domain.java`、`controller.java`、`index.vue`、`api.js`、`sql.vm` 结果等），前端用 Tabs 展示，并支持复制。

### 1.1 生成代码

列表勾选后点 `生成`，或行内下载图标：

- **`genType = 0`（默认，zip）**：  
  `$download.zip('/tool/gen/batchGenCode?tables=' + names, 'xxx.zip')`  
  浏览器下载压缩包，内含按模板展开的前后端与 SQL 文件。
- **`genType = 1`（自定义路径）**：  
  `GET /tool/gen/genCode/{tableName}`  
  由后端直接写入配置的 `genPath`（服务器本地路径）。

### 1.5 创建表

需 `admin` 角色。`创建` 打开 `createTable.vue`，粘贴一条或多条 `CREATE TABLE`，提交 `POST /tool/gen/createTable`，成功后可再走导入。

### 1.6 删除

勾选后 `删除` → `DELETE /tool/gen/{ids}`。仅删除生成配置记录，**不会删库表的**。

---

## 2. 模板

目录：`neu-generator/src/main/resources/vm/`

| 路径 | 产物 |
|------|------|
| `java/controller.java.vm` | Controller |
| `java/domain.java.vm` | 实体 |
| `java/mapper.java.vm` | Mapper 接口 |
| `java/service.java.vm` | Service 接口 |
| `java/serviceImpl.java.vm` | Service 实现 |
| `java/sub-domain.java.vm` | 子表实体 |
| `xml/mapper.xml.vm` | MyBatis XML |
| `js/api.js.vm` | 前端 API 封装 |
| `vue/index.vue.vm` | 列表页 |
| `vue/index-tree.vue.vm` | 树表页 |
| `sql/sql.vm` | 菜单等 SQL |

---

## 3. 生成结果接入工程

1. **后端**
   - 将 Controller / Service / Mapper 接口 / Domain 放入对应业务模块。
   - Mapper XML 放入该模块 `src/main/resources/mapper/...`，或确保 `mapperLocations` 能扫到。
   - 包路径与 `@RequestMapping` 与前端 `api/*.js` 一致。
   - 大整数主键（雪花 ID）在实体上增加：

     ```java
     @JsonFormat(shape = JsonFormat.Shape.STRING)
     @JSONField(serializeUsing = ToStringSerializer.class)
     private Long id;
     ```

     否则前端会丢失精度（见 `踩坑记录.md`）。
   - 若需要免登录接口，在 `SecurityConfig` 中按 `permitAll` / 白名单配置；不要对已登录场景使用 `anonymous()`。
   - 重启后端。

2. **前端**
   
   - 合并生成的 `api/*.js` 到 `src/api/`。
   - 合并 `views/**` 到 `src/views/`。
   - 执行生成的 `sql`（菜单、权限），或在 `系统管理 → 菜单管理` 手工添加。
   - 刷新浏览器重新拉取路由。
   
3. **核对 mapping**
   - 前端 URL 与 `@GetMapping` / `@RequestMapping` 一致，避免多余尾斜杠。
   - 前端调用的每个路径在 Controller 中都有对应方法。
